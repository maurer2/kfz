const ListProvider = require('../list.js');

describe('ListProvider', () => {
    describe('Constructor', () => {
        it('should be created with 1 parameter', () => {
            const lp = new ListProvider();
            expect(lt).to.have.property('countryCode');
        });
    });
});